{
  "name": "gobble-starter-project",
  "description": "quick gobble example",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run build && open index.html",
    "build": "gobble build -f dist"
  },
  "dependencies": {
    "d3": "^3.5.5"
  },
  "devDependencies": {
    "gobble": "^0.10.2",
    "gobble-babel": "^5.1.0",
    "gobble-cli": "^0.4.2",
    "gobble-esperanto-bundle": "^0.2.1"
  }
}
